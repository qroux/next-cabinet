import React from "react";

const QuestionSection = ({ children }: { children: React.ReactNode }) => {
  return <div style={{ marginBottom: "5rem" }}>{children}</div>;
};

export default QuestionSection;
